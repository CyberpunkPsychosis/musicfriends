# musicfriends

> 仓库已重置 —— 原「音乐分享交友网站」代码已归档至 git 历史，当前为干净起点。

## 现状

这个仓库原本是一个 Spring Boot 的音乐社交/交友平台 demo，现已不再沿用该方向。
旧代码全部保留在 git 历史中，可随时找回：

```bash
git log --oneline              # 查看历史提交
git checkout <旧commit> -- .   # 取回旧版本的文件
```

## 下一步

方向待重新规划。
